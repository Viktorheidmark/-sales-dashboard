# Repository Cleanup Audit

> Read-only audit. **No production code, configuration, migrations, tests, or assets were deleted, moved, renamed, formatted, or modified.** The only file created by this pass is this document.
>
> Repo: `-sales-dashboard` · Branch: `main` (clean working tree) · Tracked files: **175**
> Audit method: `git status`, `git ls-files`, `git worktree list`, dependency import tracing, and reference searches across `backend/`, `frontend/`, `mcp_server/`, `docs/`, `scripts/`, and root config. Dynamic usage (env vars, string-referenced modules, `os.environ`, subprocess, templates, CLI) was explicitly checked where relevant and is noted per item.

---

## 1. Safe to remove now

Items with strong evidence that removal is safe. All are either git-ignored generated artifacts or zero-information placeholders. Nothing here affects build, runtime, tests, or deployment.

| # | Path | What it is | Evidence unused/generated | Tracked by git? | Risk |
|---|------|-----------|---------------------------|-----------------|------|
| 1.1 | `.claude/worktrees/agent-a4016e48aa6e59dda`, `…/agent-a5f6d0367f0b932bd`, `…/agent-a718555dd4a3e2e00` | Three leftover **agent git worktrees** (~221 MB total) | `git worktree list` shows them as detached agent worktrees on throwaway branches (`worktree-agent-*`). Git-ignored via `.gitignore` (`.claude/worktrees/`). Not referenced by any code, build, or deploy step. | No (ignored) | low |
| 1.2 | All `__pycache__/` dirs (12 dirs outside `.venv`), `backend/.pytest_cache/` | Python bytecode + pytest caches | Regenerated automatically on next run/test. Git-ignored (`__pycache__/`, `*.pyc`). | No (ignored) | low |
| 1.3 | `frontend/dist/` (~912 KB) | Vite build output | Regenerated by `npm run build`. Git-ignored (`dist/`). | No (ignored) | low |
| 1.4 | `frontend/.gitkeep` (0 bytes) | Empty-directory placeholder | `frontend/` now contains a full app (`src/`, configs, `package.json`); the placeholder no longer serves any purpose. | **Yes** | low |

**Exact safe removal commands**

```bash
# 1.1 — remove stale agent worktrees (does not touch main)
git worktree remove .claude/worktrees/agent-a4016e48aa6e59dda --force
git worktree remove .claude/worktrees/agent-a5f6d0367f0b932bd --force
git worktree remove .claude/worktrees/agent-a718555dd4a3e2e00 --force
git worktree prune
# optional: delete the throwaway branches once worktrees are gone
git branch -D worktree-agent-a4016e48aa6e59dda worktree-agent-a5f6d0367f0b932bd worktree-agent-a718555dd4a3e2e00

# 1.2 / 1.3 — local generated artifacts (safe; regenerated)
find . -type d -name __pycache__ -not -path '*/.venv/*' -not -path '*/node_modules/*' -prune -exec rm -rf {} +
rm -rf backend/.pytest_cache
rm -rf frontend/dist

# 1.4 — redundant placeholder (a tracked-file change → belongs to Phase B)
git rm frontend/.gitkeep
```

> Note: 1.1–1.3 are purely local (no commit needed). 1.4 changes a tracked file and should be committed as part of **Phase B**.

---

## 2. Review before removing

Potentially obsolete items that need manual confirmation before deletion. None are referenced by imports, but each warrants a deliberate check because frontend components/utilities are not flagged by `tsc --noEmit` simply for being unimported, and IDE/launch artifacts may be intentionally shared.

| # | Path | Why it may be unused | What could break | How to verify safely | Risk |
|---|------|----------------------|------------------|----------------------|------|
| 2.1 | `frontend/src/components/sections/MarketShare.tsx` | The `MarketShare` **component** is never imported. `OverviewPage` renders `MarketPosition` instead; only the `MarketShareResponse` *type* / `getMarketShare` API / `isMarketShareChart` helpers are used elsewhere (different concerns). | A page or future feature that re-introduces this card; nothing in current routes. | `rg "import.*MarketShare\b" frontend/src` returns no component import. Confirm with `git log -- frontend/src/components/sections/MarketShare.tsx` and a product owner. Then `npx tsc --noEmit` + `npm run build`. | medium |
| 2.2 | `frontend/src/components/layout/PageHeader.tsx` | `PageHeader` is defined but never imported anywhere. | Layouts that expected a shared header (currently `PageHeader` logic lives inline / in `AppShell`). | `rg "PageHeader" frontend/src` shows only the definition. Verify no dynamic/string usage (there is none in this codebase). | medium |
| 2.3 | `frontend/src/components/ui/MetaFooter.tsx` | `MetaFooter` is defined but never imported. | Cards that wanted a shared source footer; current cards use other footers/`MetaFooter`-equivalent inline markup. | `rg "MetaFooter" frontend/src` shows only the definition. | medium |
| 2.4 | `frontend/src/utils/overviewInsight.ts` (`buildOverviewInsightText`, `overviewInsightStatusLabel`) | Both exported functions are defined but never imported/called. `OverviewHero`/`OverviewPage` do not consume them. (Contrast: `marketPositionInsight.ts` **is** used by `MarketPosition.tsx` — keep that one.) | A future overview narrative feature. | `rg "buildOverviewInsightText|overviewInsightStatusLabel" frontend/src` shows only definitions. | medium |
| 2.5 | `analytics_shadow_eval_enabled()` in `backend/app/analytics/flags.py` | The helper is defined but has **no callers** (shadow-eval mode was never wired into the chat path). | Nothing at runtime — it is never invoked. **Do NOT remove the `analytics_shadow_eval` settings field** (see §3) — only the unused helper function is a candidate. | `rg "analytics_shadow_eval_enabled" backend` shows only the definition. | medium |
| 2.6 | `.claude/launch.json`, `frontend/.claude/launch.json` | Tracked IDE/agent debug-launch configs (npm `dev` runners). They are tool artifacts, not app code. | A teammate who relies on the committed launch profile. | Confirm with the team whether launch configs should be shared. If not, remove and add `.claude/` to `.gitignore` (currently only `.claude/worktrees/` is ignored). | medium |
| 2.7 | `backend/alembic/versions/.gitkeep` | Placeholder is redundant now that real migrations exist beside it (`754a14f93db0_…`, `b8e3f7a2c145_…`). | Nothing — Alembic ignores it. Harmless if kept. | Visual: the directory is non-empty. Removal is cosmetic only. | medium (kept conservatively because it lives inside the migrations tree) |

---

## 3. Keep

Files/folders that may look removable but **must remain**. These are explicitly protected by the audit's safety rules.

| Area | Paths | Reason to keep |
|------|-------|----------------|
| **Alembic migrations** | `backend/alembic/`, `backend/alembic.ini`, `backend/alembic/env.py`, `backend/alembic/versions/754a14f93db0_create_sales_analytics_schema.py`, `…/b8e3f7a2c145_add_users_table.py` | Define and version the production DB schema. Never delete migrations even if "unreferenced" by app imports. |
| **Legacy chat routing / fallback** | `backend/app/services/chat.py`, `tool_planner.py`, `planner_service.py`, `intent_router.py`, `comparison_labels.py`, `follow_up_*.py`, `decline_period.py`, `services/chart_builder.py` | The legacy path still serves all non-migrated intents. The new orchestrator only handles explicit period comparison (gated by `AI_ORCHESTRATED_ANALYTICS_ENABLED`, default off). Removing legacy code would break everything else. Note: `services/chart_builder.py` (legacy) and `analytics/chart_builder.py` (orchestration) are **intentionally distinct**, both in use. |
| **Analytics orchestration** | `backend/app/analytics/` (`orchestrator`, `planner`, `validator`, `executor`, `registry`, `verifier`, `chart_builder`, `responder`, `labels`, `schemas`, `flags`) | New canonical pipeline. Every module has ≥1 non-test importer (verified). Gated behind a feature flag for safe rollout. |
| **MCP server** | `mcp_server/` (`server.py`, `db.py`, `query_helpers.py`, `__init__.py`, `smoke_test.py`) | Runtime query layer (`FastMCP`). Imported by backend services and tests. |
| **Database models** | `backend/app/models/*` | SQLAlchemy ORM mapped to the live schema; imported by Alembic env, services, and seed script. |
| **Seed / demo data** | `backend/scripts/seed_demo_data.py` | Populates the demo tenant dataset used by DEMO.md and local runs. |
| **Tests + smoke scripts** | `backend/tests/*`, `backend/scripts/*_smoke_test.py`, `weekly_chart_regression.py`, `mcp_server/smoke_test.py` | Test infrastructure and manually/CI-invoked CLI scripts. Several import `httpx`/`uvicorn` only here — that is the reason those deps exist. |
| **Docs / DEMO / CHECKLIST** | `README.md`, `DEMO.md`, `CHECKLIST.md`, `docs/analytics-orchestration-audit.md` | Single canonical copy of each — **no duplicates found**. Keep. |
| **Deployment / build config** | `frontend/vite.config.ts`, `tsconfig.json`, `tailwind.config.js`, `postcss.config.js`, `index.html`, `package.json`, `package-lock.json`, `backend/requirements.txt`, `backend/alembic.ini` | Required to build/run/deploy. |
| **Environment templates** | `.env.example`, `frontend/.env.example` | Onboarding templates. Keep (and keep tracked). The real `.env` files are git-ignored (see §4). |
| **Settings fields read via `os.environ`** | `chat_include_analysis_meta`, `analytics_shadow_eval` in `backend/app/config.py` | `Settings` uses `extra="forbid"`, so every key present in `.env`/`.env.example` (`CHAT_INCLUDE_ANALYSIS_META`, `ANALYTICS_SHADOW_EVAL`) **must** have a matching field or app startup fails. Keep even though `chat_include_analysis_meta` is read via `os.environ` in `chat.py`. |

---

## 4. Git hygiene findings

| Item | Status | Action |
|------|--------|--------|
| `.claude/` (root) | Partially ignored — only `.claude/worktrees/` is in `.gitignore`. `.claude/launch.json` is **tracked**; `.claude/settings.local.json` is ignored. | Decide on policy (see §2.6). Consider broadening `.gitignore` to `.claude/` (keeping `!.claude/launch.json` if shared on purpose). |
| `.cursor/` | Not present in repo tree. | No action. |
| Git worktrees | `.claude/worktrees/*` correctly git-ignored; 3 stale agent worktrees present locally. | **Safe to remove locally** (see §1.1). |
| `backend/.venv/` (~277 MB) | Correctly git-ignored (`.venv`). | OK. Local only. |
| `frontend/node_modules/` (~115 MB) | Correctly git-ignored (`node_modules/`). | OK. Local only. |
| `frontend/dist/` (~912 KB) | Correctly git-ignored (`dist/`). | Safe to remove locally; regenerated. |
| `__pycache__/`, `*.pyc` | Correctly git-ignored. | Safe to remove locally. |
| `backend/.pytest_cache/` | Correctly git-ignored (matched by `.venv`? — actually not; verify). | **Add `.pytest_cache/` to `.gitignore`** to be explicit (currently relies on it being untracked). Safe to remove locally. |
| Local PDFs / screenshots / logs / `.zip` / `.csv` / `.sqlite` | **None found** anywhere in the working tree. | No action. |
| `.DS_Store` | Ignored (`*.DS_Store` via `.DS_Store`); none currently tracked. | OK. |
| Tracked binary assets (pdf/png/jpg/svg/zip/db/log) | **None tracked** (`git ls-files` shows zero binary assets). | OK. |
| Accidental secrets | `.env` and `frontend/.env` are git-ignored; only `.env.example` templates are tracked. **No secrets committed.** | OK — do not commit `.env`. |
| Commit message hygiene | Recent history has terse/non-descriptive messages (`f`, `h`, `chatt`, `big frontend fix`). | Informational only — not a cleanup action. Consider a commit-message convention going forward. |

> `.pytest_cache/` is the one gap: it is currently untracked but not explicitly listed in `.gitignore`. Recommend adding it so it can never be committed accidentally.

---

## 5. Unused dependencies

> **No dependencies were removed.** Every declared dependency was traced to at least one import or runtime usage.

### Frontend (`frontend/package.json`)

**dependencies**

| Package | Verdict | Evidence |
|---------|---------|----------|
| `react`, `react-dom` | Used | App entry, all components. |
| `react-router-dom` | Used | `App.tsx` routing. |
| `react-markdown` | Used | `ChatPanel.tsx` (and chart/section components). |
| `recharts` | Used | `MiniAssistantChart`, `SalesTrend`, `MarketShare`, `MarketPosition`. |

**devDependencies**

| Package | Verdict | Evidence |
|---------|---------|----------|
| `@types/react`, `@types/react-dom` | Used | Type checking. |
| `@vitejs/plugin-react` | Used | `vite.config.ts`. |
| `vite` | Used | dev/build/preview scripts. |
| `typescript` | Used | `build` script (`tsc && vite build`), `tsc --noEmit`. |
| `tailwindcss`, `postcss`, `autoprefixer` | Used | `tailwind.config.js`, `postcss.config.js`. |

- **Definitely unused:** none.
- **Probably unused:** none.
- **Uncertain / dynamic:** none.
- **Duplicate chart/date/UI libraries:** none — single chart lib (`recharts`), no competing date or UI kit.

### Backend (`backend/requirements.txt`)

| Package | Verdict | Evidence |
|---------|---------|----------|
| `fastapi`, `uvicorn[standard]` | Used | `app/main.py`; `uvicorn` also driven by smoke scripts. |
| `sqlalchemy` | Used | models, `database.py`. |
| `alembic` | Used | migrations, `alembic/env.py`. |
| `psycopg[binary]` | Used | `DATABASE_URL` driver (`postgresql+psycopg://`). |
| `pydantic-settings` | Used | `app/config.py`. |
| `python-dotenv` | Used | `mcp_server/db.py`, `backend/alembic/env.py`. |
| `mcp[cli]` | Used | `mcp_server/server.py` (`from mcp.server.fastmcp import FastMCP`). |
| `httpx` | Used | smoke-test scripts (`insights_smoke_test.py`, `regression_swedish_smoke_test.py`, …). Test/runtime-tooling dependency. |
| `openai` | Used | `app/services/chat.py` (`from openai import AsyncOpenAI, OpenAI`). |
| `python-jose[cryptography]` | Used | `app/services/auth.py`, `app/dependencies.py`. |
| `bcrypt` | Used | `app/services/auth.py` (password hashing). |
| `reportlab` | Used | `app/services/pdf_builder.py`. |
| `matplotlib` | Used | `pdf_builder.py`, `tenant_theme.py`, `pdf_smoke_test.py`. |

- **Definitely unused:** none.
- **Probably unused:** none.
- **Uncertain / dynamic:** none (`httpx` is used only by scripts; keep — it backs the smoke/regression CLI tooling).

---

## 6. Dead code candidates

For each: the references searched and whether dynamic use was checked. None should be removed automatically — all are listed for confirmation in §2.

| Candidate | Type | References searched | Dynamic use checked? | Verdict |
|-----------|------|---------------------|----------------------|---------|
| `frontend/src/components/sections/MarketShare.tsx` (`MarketShare` component) | Unused React component | `rg "import.*MarketShare\b"`, `rg "<MarketShare"` → no importers/JSX usage | Yes — no string/dynamic references; no barrel/index re-export | Candidate (medium) |
| `frontend/src/components/layout/PageHeader.tsx` (`PageHeader`) | Unused React component | `rg "PageHeader"` → definition only | Yes — none | Candidate (medium) |
| `frontend/src/components/ui/MetaFooter.tsx` (`MetaFooter`) | Unused React component | `rg "MetaFooter"` → definition only | Yes — none | Candidate (medium) |
| `frontend/src/utils/overviewInsight.ts` (`buildOverviewInsightText`, `overviewInsightStatusLabel`) | Unused utility module | `rg "buildOverviewInsightText|overviewInsightStatusLabel"` → definitions only | Yes — none | Candidate (medium) |
| `analytics_shadow_eval_enabled()` (`backend/app/analytics/flags.py`) | Unused helper function | `rg "analytics_shadow_eval_enabled"` → definition only | Yes — checked `os.environ`/string usage; only the config **field** is read at startup, not this helper | Candidate (medium); keep the `analytics_shadow_eval` field |

**Routes / API endpoints / services checked — all in use (no dead code):**

- **Backend routers** (`auth`, `dashboard`, `suppliers`, `chat`, `insights`) — all registered in `app/main.py`. None unused.
- **Backend services** — every module in `app/services/` has ≥1 non-test importer (verified by import-count sweep). None orphaned.
- **Analytics modules** — every module in `app/analytics/` has ≥1 non-test importer. None orphaned.
- **Scripts** — all `backend/scripts/*` are CLI/smoke tooling (intentionally standalone). Keep.
- **Feature flags** — `AI_ORCHESTRATED_ANALYTICS_ENABLED` and `ANALYTICS_DEBUG_TRACE` are actively read; `ANALYTICS_SHADOW_EVAL` field is required for `.env` parsing (`extra="forbid"`) even though its helper is currently uncalled. No flags are safe to delete.

---

## 7. Proposed cleanup phases

Each phase is independent and ordered low→high risk. Run the verification block (below) after each phase.

### Phase A — Safe local cleanup only (no git changes)
- **Files:** `__pycache__/` dirs, `backend/.pytest_cache/`, `frontend/dist/`, the 3 stale worktrees under `.claude/worktrees/`.
- **Risk:** very low (all git-ignored / regenerable).
- **Tests after:** `cd backend && python -m unittest discover -s tests`; `cd frontend && npm run build` (regenerates `dist/`).
- **Rollback:** none needed — caches/build regenerate; worktrees removed via `git worktree remove` are recreatable on demand.

### Phase B — Safe git cleanup (tracked-file + ignore hygiene)
- **Files:** `git rm frontend/.gitkeep`; add `.pytest_cache/` to `.gitignore`; (optional, after team sign-off from §2.6) broaden `.claude/` ignore and `git rm --cached .claude/launch.json frontend/.claude/launch.json`.
- **Risk:** low.
- **Tests after:** `git status` (confirm only intended files staged); `npm run build`.
- **Rollback:** `git restore --staged <file>` / `git checkout -- <file>` before commit, or `git revert` the commit after.

### Phase C — Dependency cleanup
- **Files:** none. Audit found **no removable dependencies** (frontend or backend).
- **Risk:** n/a.
- **Tests after:** n/a.
- **Rollback:** n/a.

### Phase D — Dead code removal (requires confirmation)
- **Files:** §2.1 `MarketShare.tsx`, §2.2 `PageHeader.tsx`, §2.3 `MetaFooter.tsx`, §2.4 `overviewInsight.ts`, §2.5 the `analytics_shadow_eval_enabled()` helper (leave the config field).
- **Risk:** medium — confirm no planned re-use; these are real source files.
- **Tests after:** `cd frontend && npx tsc --noEmit && npm run build`; `cd backend && python -m unittest discover -s tests`.
- **Rollback:** `git revert` the removal commit (keep each removal a separate, small commit for clean revert).

### Phase E — Documentation cleanup
- **Files:** none required — `README.md`, `DEMO.md`, `CHECKLIST.md`, `docs/analytics-orchestration-audit.md` are each unique and current. Optional: cross-link this audit from `README.md`.
- **Risk:** very low.
- **Tests after:** none (docs only).
- **Rollback:** `git revert`.

---

## Verification commands

Run after **every** phase that changes tracked files or code:

```bash
git status
git ls-files
cd backend && python -m unittest discover -s tests
cd frontend && npx tsc --noEmit
cd frontend && npm run build
```

Inspect history/state before concluding anything:

```bash
git log --oneline -20
git worktree list
git status --ignored --porcelain
```

---

## Final summary

1. **Safe-to-remove items (count): 4 groups** — stale agent worktrees (§1.1), Python caches `__pycache__`/`.pytest_cache` (§1.2), `frontend/dist/` (§1.3), and the redundant `frontend/.gitkeep` (§1.4). All git-ignored or zero-information; only `.gitkeep` is a tracked change.

2. **Review-required items (count): 7** — `MarketShare.tsx`, `PageHeader.tsx`, `MetaFooter.tsx`, `overviewInsight.ts`, `analytics_shadow_eval_enabled()`, the tracked `.claude/launch.json` pair, and `alembic/versions/.gitkeep`.

3. **Top 10 highest-value cleanup opportunities**
   1. Remove the 3 stale agent worktrees under `.claude/worktrees/` — reclaims **~221 MB** locally.
   2. Clear `backend/.venv` build leftovers and `__pycache__`/`.pytest_cache` — regenerable, frees disk.
   3. Delete `frontend/dist/` build output locally (~912 KB; regenerated by `npm run build`).
   4. Remove the unused `MarketShare.tsx` component (superseded by `MarketPosition`).
   5. Remove the unused `overviewInsight.ts` utility module.
   6. Remove the unused `PageHeader.tsx` layout component.
   7. Remove the unused `MetaFooter.tsx` UI component.
   8. Drop the dead `analytics_shadow_eval_enabled()` helper (keep the config field).
   9. Add `.pytest_cache/` to `.gitignore` and remove the now-redundant `frontend/.gitkeep`.
   10. Decide policy on the tracked `.claude/launch.json` files and tighten `.claude/` ignore rules.

4. **Recommended first cleanup phase:** **Phase A — Safe local cleanup only.** It is zero-risk (everything is git-ignored or regenerable), requires no commit, and immediately reclaims ~220 MB+ of disk via the stale worktrees.

5. **Confirmation:** No files were deleted, moved, renamed, formatted, or otherwise changed by this audit. The **only** file written is this document, `docs/repository-cleanup-audit.md`. Working tree of `main` remains clean apart from this new file.
